export const BASE_URL = "https://breaking-api.alpha.tv2.no/v1/public";

export const DEFAULT_STALE_TIME = 10_000;

export const QUERY_KEYS = {
  ALL_PORTALS: "portals",
  PORTAL: "portal",
} as const;
